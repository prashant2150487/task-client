# name: Good-task CI

# on:
#   push:
#     branches: [ main, live ]
#     tags: ['v*']
#   pull_request:
#     branches: [ main, live ]

# jobs:
#   build:
#     name: Install & Build Good-task
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Use Node.js 22
#         uses: actions/setup-node@v4
#         with:
#           node-version: '22'

#       - name: Cache node_modules
#         uses: actions/cache@v4
#         with:
#           path: node_modules
#           key: ${{ runner.os }}-good-task-node-22-${{ hashFiles('package.json') }}
#           restore-keys: |
#             ${{ runner.os }}-good-task-node-22-

#       - name: Install dependencies
#         run: npm ci

#       - name: Run lint
#         run: npm run lint

#       - name: Build Good-task
#         run: npm run build

#       - name: Upload build artifact
#         uses: actions/upload-artifact@v4
#         with:
#           name: good-task-dist-${{ github.sha }}
#           path: dist
#           retention-days: 5

#   publish:
#     name: Publish Good-task (on tag)
#     needs: build
#     if: startsWith(github.ref, 'refs/tags/')
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Use Node.js 22
#         uses: actions/setup-node@v4
#         with:
#           node-version: '22'

#       - name: Install dependencies
#         run: npm ci

#       - name: Build for production
#         run: npm run build

#       - name: Publish placeholder
#         run: |
#           echo "✅ Tag detected: ${GITHUB_REF}"
#           echo "Add publish/deploy steps here:"
#           echo "  - Deploy to Vercel/Netlify"
#           echo "  - Upload to S3 + CloudFront"
#           echo "  - Deploy to GitHub Pages"
#           echo "Use GitHub Secrets for authentication"
